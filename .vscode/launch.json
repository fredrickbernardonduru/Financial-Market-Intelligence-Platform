{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Pipeline",
      "type": "python",
      "request": "launch",
      "module": "etl.pipeline",
      "cwd": "${workspaceFolder}"
    }
  ]
}